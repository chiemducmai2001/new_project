import 'package:flutter/material.dart';

class UserAddTikTok extends StatelessWidget {
  const UserAddTikTok({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Add'),
    );
  }
}
