import 'package:flutter/material.dart';

class UserSearchTikTok extends StatelessWidget {
  const UserSearchTikTok({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Search'),
    );
  }
}
