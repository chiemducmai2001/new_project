import 'package:flutter/material.dart';

class UserAccountTikTok extends StatelessWidget {
  const UserAccountTikTok({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Account TikTok'),
    );
  }
}
