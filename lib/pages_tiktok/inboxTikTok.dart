import 'package:flutter/material.dart';

class UserInboxTikTok extends StatelessWidget {
  const UserInboxTikTok({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Search'),
    );
  }
}
